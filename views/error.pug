doctype html
html(lang='en')
    include common/head.pug
    +head("Our valued customer")

    body
      h1= message
      h2= error.status
      pre #{error.stack}
      include common/footer.pug 
      +footer()
